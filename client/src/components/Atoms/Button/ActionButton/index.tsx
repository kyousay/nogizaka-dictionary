import {ButtonFactory} from '../index'
import * as utilStyle from '../../../../util/styles'

const ActionStyle = {
    bgColor: utilStyle.color.purple,
}

const ActionButton = ButtonFactory(ActionStyle)

  export default ActionButton